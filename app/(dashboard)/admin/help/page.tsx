export const dynamic = "force-dynamic";

import { HelpGuide } from "@/components/dashboard/help-guide";

export const metadata = {
  title: "Help Guide — CLM Admin",
};

export default function HelpPage() {
  return <HelpGuide />;
}
